#####################################################################
#   print_start macro
#####################################################################

## *** THINGS TO UNCOMMENT: ***
## Bed mesh (2 lines at 2 locations)
## Z_TILT_ADJUST if your printer is a Trident
## Quad gantry level if your printer is a V2
## Nevermore - if you have one

[gcode_macro MAYBE_HOME]
gcode:
    {% if "xyz" not in printer.toolhead.homed_axes %}
        G28
    {% endif %}

[gcode_macro PRINT_START]
gcode:
  # This part fetches data from your slicer. Such as bed temp, extruder temp, chamber temp and size of your printer.
  {% set target_bed = params.BED|int %}
  {% set target_extruder = params.EXTRUDER|int %}
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}
  
  # Homes the printer, sets absolute positioning and updates the Stealthburner leds.
  STATUS_HOMING         # Sets SB-leds to homing-mode
  G28                   # Full home (XYZ)
  G90                   # Absolut position

  ##  Uncomment for bed mesh (1 of 2)
  BED_MESH_CLEAR       # Clears old saved bed mesh (if any)

  # Filament Type chamber setting
  {% if params.FILAMENT is defined %} 
    {% set filament = params.FILAMENT|default("NONE")|string %}
    {% if filament != 'NONE' %}
      {% set fct = ({"ASA": 50, "ABS": 50, "PLA": 0, "PETG": 0}) %} #Adjust as needed
      {% set chamber = fct[filament] %}
      { action_respond_info("Filament: " + filament|string + ", Chamber Temp: " + chamber|string + ", Preheat Bed Temp: " + target_bed|string) }
      {% set target_chamber = printer["temperature_sensor chamber_endstop"].temperature|float %}
      {% if target_chamber < chamber|int %}
        STATUS_HEATING  
        RESPOND MSG="Heating chamber..."
        M140 S{target_bed|int} #set bed temp to reach desired chamber temp
        M106 S255 #enable fan 100% to spread the heat better

        ##  Uncomment if you have a Nevermore.
        #SET_PIN PIN=nevermore VALUE=1                      # Turns on the nevermore
        
        # Heating nozzle to 150 degrees. This helps with getting a correct Z-home
        SET_DISPLAY_TEXT MSG="Hotend: 150c"          # Displays info
        M104 S{150} #preheat extruder while waiting
        
        MAYBE_HOME
        G0 X{printer.toolhead.axis_maximum.x/2} Y{printer.toolhead.axis_maximum.y/2} Z{20} F6000 #Go to center of bed
        TEMPERATURE_WAIT SENSOR="temperature_sensor chamber_endstop" MINIMUM={chamber|int} #Wait for chamber Temp
        M117 Finished Heating chamber... 
        RESPOND MSG="Finished Heating chamber..." #can be enabled if RESPOND feature is enabled
      {% else %}
        M117 Chamber already heated.
        SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Displays info
        STATUS_HEATING                                      # Sets SB-leds to heating-mode
        G1 X{x_wait} Y{y_wait} Z15 F9000                    # Goes to center of the bed
        M190 S{target_bed}                                  # Sets the target temp for the bed
        SET_DISPLAY_TEXT MSG="Soak for 5min"                # Displays info
        G4 P300000                                          # Waits 5 min for the bedtemp to stabilize
      {% endif %}
    {% else %}
      M117 Issue with Slicer Filament Detection. 
      RESPOND MSG="Issue with Slicer Filament Detection." #can be enabled if RESPOND feature is enabled
    {% endif %}
  {% endif %}

  ##  Uncomment for Trident (Z_TILT_ADJUST)
  SET_DISPLAY_TEXT MSG="Z-tilt adjust"     # Displays info
  STATUS_LEVELING                          # Sets SB-leds to leveling-mode
  Z_TILT_ADJUST                            # Levels the buildplate via z_tilt_adjust
  G28 Z                                    # Homes Z again after z_tilt_adjust

  ##  Uncomment for bed mesh (2 of 2)
  SET_DISPLAY_TEXT MSG="Bed mesh"    # Displays info
  STATUS_MESHING                     # Sets SB-leds to bed mesh-mode
  BED_MESH_CALIBRATE ADAPTIVE=1      # Starts bed mesh

  # Heats up the nozzle up to target via data from slicer
  SET_DISPLAY_TEXT MSG="Heating hotend to: {target_extruder}c"  # Displays info
  STATUS_HEATING                                                # Sets SB-leds to heating-mode
  G1 X{x_wait} Y{y_wait} Z15 F9000                              # Goes to center of the bed
  M107                                                          # Turns off partcooling fan
  M109 S{target_extruder}                                       # Heats the nozzle to printing temp

  # Gets ready to print by doing a purge line and updating the SB-leds
  SET_DISPLAY_TEXT MSG="Printer goes brr"          # Displays info
  STATUS_PRINTING                                  # Sets SB-leds to printing-mode
  LINE_PURGE
  #G0 X{x_wait - 50} Y4 F10000                      # Moves to starting point
  #G0 Z0.4                                          # Raises Z to 0.4
  #G91                                              # Incremental positioning 
  #G1 X100 E20 F1000                                # Purge line
  G90                                               # Absolut position


[gcode_macro PRINT_END]
gcode:
  M106 S0 ; Turn Fan Off
  M104 S0 ; turn off extruder
  M140 S0 ; turn off bed
  G91 ; change to relative
  G1 Z20 E-2 F3000; Raise Z 30mm
  G90 ; switch back to absolute mode
  G28 X0 Y0 ;move X/Y to min endstops
  M84 ; disable motors
